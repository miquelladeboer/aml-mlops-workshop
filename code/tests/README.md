# Folder for test organization

## Naming

* Multiple testing frameworks, we choose pytest
* Init files to create modules
* Note that python modules cannot have dashes in the name
* Test cases start with 'test_'
